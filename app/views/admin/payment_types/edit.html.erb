<% page_name = @payment_type.name %>
<%= title page_name %>

<%= content_tag :h2, "Editing #{page_name}" %>

<%= render 'form' %>

<%= link_to 'Show', [:admin, @payment_type] %> |
<%= link_to 'Back', admin_payment_types_path %>

<%= content_for :left do %>
  <%= render "admin/shared/menu_links" %>
<% end %>

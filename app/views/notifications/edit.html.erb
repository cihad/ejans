<% page_name = @notification.title %>
<%= title page_name %>
<%
  breadcrumbs.add t("services.name"), services_path
  breadcrumbs.add @service.title, service_path(@service)
  breadcrumbs.add @notification.title, service_notification_path(@service, @notification)
  breadcrumbs.add t("global.edit")
%>
<%= content_tag :h2, "Editing #{page_name}" %>

<%= render 'form' %>
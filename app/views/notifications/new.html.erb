<% page_name = t(".new_notification") %>
<% provide :title, page_name %>
<%
  breadcrumbs.add t("services.name"), services_path
  breadcrumbs.add @service.title, service_path(@service)
  breadcrumbs.add page_name
%>
<%= content_tag :h2, page_name %>

<%= render 'form' %>
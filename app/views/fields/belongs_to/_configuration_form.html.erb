<%= simple_check_box f, :filter %>

<%#= simple_check_box f, :can_be_added_only_by_belongs_to_node_author %>

<%= simple_select f,
      :parent_node_node_type_id,
      NodeType.all.not.where(id: @node_type.id).map { |nt| [nt.name, nt.id] } %>
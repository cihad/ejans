<%= simple_check_box f,
      :filter,
      input: { data: { toggle: "filter" } } %>

<%= simple_number_field f, :level %>

<%= simple_text_field f,
      :top_place_name,
      input: { 
        data: 
          { provide: "typeahead",
            source_url: find_by_name_places_path 
          },
        autocomplete: "off"
        } %>

<%= simple_check_box f, :place_page_list %>
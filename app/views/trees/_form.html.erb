<%= horizontal_form_for @parent_item do |f| %>
  <% unless @parent_item.root? %>
    <%= f.input :parent_node_name do %>
      <span class="span4 uneditable-input"><%= @parent_item.parent.name %></span>
    <% end %>
  <% end %>

  <%= f.input :name, input_html: { class: 'span4' } %>
  <%= f.input :childs, as: :text, input_html: { rows: 10, class: 'span4' } %>
  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
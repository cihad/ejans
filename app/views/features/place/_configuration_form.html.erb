<%= simple_check_box f,
      :filter,
      input: { data: { toggle: "filter" } } %>

<%= simple_number_field f, :level %>

<%= simple_select f,
      :top_place,
      Place.top_places.map { |p| [p.name, p.id]} %>
<%= form_for  [@node_type, node], 
              url: node_type_node_path(@node_type, node) do |f| %>
  <%= f.submit "onayla", class: "btn btn-mini", name: "#{@node_type.form_parameterize}[approve]" %>
<% end %>
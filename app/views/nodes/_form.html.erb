<%= form_for [@node_type, @node] do |f| %>
  <% if @node.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@node.errors.count, "error") %> prohibited this node from being saved:</h2>

      <ul>
      <% @node.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <table class="table table-form">
    <%= f.hidden_field :node_type_id %>
    <%= simple_text_field f, :title %>

    <%= f.fields_for :features do |feature| %>
      <%= feature_form_for(feature) %>
    <% end %>

    <%= simple_submit f %>
  </table>
<% end %>
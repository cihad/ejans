<% page_name = @node.title %>
<% provide :title, page_name %>
<% meta_description @node.title %>
<%= page_title @node.title %>
<%= body_class 'node-page' %>

<%= render "node" %>

<div id="comments">
  <h3>Comments</h3>
  <%= render @node.comments %>
  <%= render 'comments/form' %>
</div>

<%= node_type_background %>
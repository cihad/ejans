<%= select_tag  field.machine_name,
                options_for_select(field.options.inject({}) { |hash, option| hash[option.name] = option.id; hash }, params["#{field.machine_name}"]),
                multiple: true,
                class: field.machine_name %>

<%= multiselect ".#{field.machine_name}", width: 170 %>
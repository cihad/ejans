<%= simple_check_box f,
      :filter,
      label: { name: t('fields.filter') } %>

<%= simple_select f,
      :category_id,
      Category.roots.map { |p| [p.name, p.id] },
      label: { name: t('fields.tree_category.category') } %>
<% if field.text_field? %>

  <%= field.f.input field.form_key, hint: field.hint, label: field.label %>

<% else %>

  <%= field.f.input field.form_key, as: :text, hint: field.hint, label: field.label,
                    input_html: { rows: field.row, class: "input-xlarge" } %>
  
<% end %>
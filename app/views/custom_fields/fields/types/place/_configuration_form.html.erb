<%= f.input :filter, as: :boolean %>

<%= f.input :multiselect, as: :boolean %>

<%= f.input :level, as: :integer %>

<%= f.input :top_place_name,
            input_html: { 
              data: {
                provide: "typeahead",
                source_url: find_by_name_places_path
              },
              autocomplete: "off"
            } %>

<%= f.input :place_page_list, as: :boolean %>
<% page_name = "Editing Service" %>
<%= title page_name %>
<% 
  breadcrumbs.add @service.title, service_path(@service)
  breadcrumbs.add page_name
%>

<%= content_tag :h2, "Edit \"#{@service.title}\" Service" %>

<%= render 'form' %>
